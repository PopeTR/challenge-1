import React from 'react';
const outputs = (props) => {
    return (
        <div className="Output">
            <p>{props.username}</p>
            <p>This is a paragraph!</p>
        </div>
    )
}

export default outputs;